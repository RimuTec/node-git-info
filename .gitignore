node_modules
build
coverage
.pnpm-store
git.properties
tmp
src/publish
# While we don't have the following in this repo, we need to ignore it as it is
# generated by the github action as part of publishing the package:
.npmrc
